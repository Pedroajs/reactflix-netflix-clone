import { useEffect } from "react";
import tmdb from "./tmdb";

export default () =>{
  useEffect(()=>{
    const loadAll = async () =>{

    }

    loadAll();
  }, []);

  return(
    <div>
      Olá Mundo
    </div>

  )
}